package com.example.myapplication;

public class Clasesita {
    public string getCadenita() {
        return cadenita;
    }

    public void setCadenita(string cadenita) {
        this.cadenita = cadenita;
    }

    private string cadenita;
    public int vocales(){
            int cuantas = cadenita.length();
            int peques = 0;

            for (int i = 0 ; i < cuantas ; ++i);
            if (cadenita.charAt(1) == 'a' ||
            if (cadenita.charAt(1) == 'e' ||
            if (cadenita.charAt(1) == 'i' ||
            if (cadenita.charAt(1) == 'o' ||
            if (cadenita.charAt(1) == 'u' );

            ++peques
                    return peques;
    }
}
